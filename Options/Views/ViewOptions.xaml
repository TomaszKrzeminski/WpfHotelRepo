<UserControl x:Class="OptionsModule.Views.ViewOptions"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OptionsModule.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Vertical" VerticalAlignment="Top">
            <Button Margin="20,10" Command="{Binding Path=Reserve}">Rezerwuj</Button>
            <Button Margin="20,10" Command="{Binding Path=Calendar}">Kalendarz</Button>
            <Button Margin="20,10" Command="{Binding Path=Statistics}">Statystyki</Button>
            <Button Margin="20,10" Command="{Binding Path=Edit}">Edytuj</Button>
        </StackPanel>
    </Grid>
</UserControl>
